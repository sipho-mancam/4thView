#pragma once
#include "kevents.hpp"
#include <QtWidgets/QWidget>

/**
* @brief
* This class handles the data exchange between the GUI elements and the incoming stream data.
* This base class is what generally stands between a call back function and the gui elements
*/
class DataStoreBase
{
public:
	//virtual void updateData(json) = 0;
	//virtual void updateViews() = 0;
	//virtual void appDataCallback();

protected:
	
};
